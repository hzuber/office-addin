import React from "react";
import { Provider } from "../../components/ui/provider";

export default function App() {
  return (
    <Provider>
      {/* <Component {...pageProps} /> */}
      <p>Hi</p>
    </Provider>
  );
}
